​

目录

收起

引言

方法：基于LLM的强化学习

RL提示词集合构建

长思维链监督微调（Long-CoT SFT）

强化学习（RL）

Long2short：面向Short-CoT模型的上下文压缩

训练细节补充

RL基础设施

实验

评估

主要结果

长上下文扩展

Long2short

消融研究

小结

附录B：预训练

语言数据

多模态数据

模型架构

原文地址

几乎和[DeepSeek-R1](https://zhida.zhihu.com/search?content_id=252988821&content_type=Article&match_order=1&q=DeepSeek-R1&zhida_source=entity)同时，月之暗面也发布了自己的推理模型**Kimi K1.5**。

早在[OpenAI-o1-preview](https://zhida.zhihu.com/search?content_id=252988821&content_type=Article&match_order=1&q=OpenAI-o1-preview&zhida_source=entity)刚发布的时候，我就强烈看好**文理分科**的趋势，也在陆续一些评论文章中表示强烈看好文理分科后国内大模型在推理模型分支上的表现。因为：

**毕竟在“东亚卷王”面前，全世界在理工科方面没有一个能打的。**

![](https://picx.zhimg.com/v2-53117b38d952a63ee3f59bffca06505d_r.jpg)

中美数学奥赛参赛选手合影

Kimi K1.5采用了**长文本上下文扩展**和**改进的策略优化方法**作为核心技术，建立了一个简洁而高效的**强化学习（RL）**框架，无需依赖**蒙特卡罗树搜索（MCST）**、价值函数和**过程奖励模型（PRM）**等复杂技术。

实验结果表明，该系统在多个基准测试和模态上均达到了领先水平：

在[AIME测试](https://zhida.zhihu.com/search?content_id=252988821&content_type=Article&match_order=1&q=AIME%E6%B5%8B%E8%AF%95&zhida_source=entity)中得分77.5分，MATH 500测试中得分96.2分，在[Codeforces](https://zhida.zhihu.com/search?content_id=252988821&content_type=Article&match_order=1&q=Codeforces&zhida_source=entity)平台上位居第94百分位，在[MathVista测试](https://zhida.zhihu.com/search?content_id=252988821&content_type=Article&match_order=1&q=MathVista%E6%B5%8B%E8%AF%95&zhida_source=entity)中得分74.9分，这些成绩与OpenAI的o1模型相当。

![](https://pic4.zhimg.com/v2-e2d99b1c1abae39360bf7768f3e2ff8b_r.jpg)

图1：Kimi K1.5 long-CoT测试结果

此外，研究团队提出的**long2short**方法创新性地运用**long-CoT（长思维链）**技术来优化**short-CoT（短思维链）**模型，在多个测试中取得了突破性成果：

AIME测试达到60.8分，[MATH500测试](https://zhida.zhihu.com/search?content_id=252988821&content_type=Article&match_order=1&q=MATH500%E6%B5%8B%E8%AF%95&zhida_source=entity)达到94.6分，LiveCodeBench测试达到47.3分，显著超越了GPT-4和Claude 3.5 Sonnet等现有short-CoT模型，性能提升最高达550%。

![](https://pica.zhimg.com/v2-73f4d2aa745e04dfca0adc9b312c7582_r.jpg)

图2：Kimi k1.5 short-CoT测试结果

  

> \*笔者会用Agent翻译形成初稿，然后精读后完成终稿，力求每一句话自己都能理解后再输出译文  
>   
> \*需要原文的请至文末跳转至原文链接阅读。

引言
--

在Scaling Law的研究背景下，基于next token预测任务的语言模型预训练表明，通过同比例扩展模型参数和数据规模可以持续提升模型智能水平。然而，这种方法受限于高质量训练数据的可用量。

本研究报告介绍了多模态LLM模型Kimi k1.5的训练方案，该模型采用RL方法训练。研究目标是探索可持续扩展的新维度。通过将RL应用于LLM，模型可以通过奖励机制进行主动探索学习，突破了静态数据集的限制。

k1.5的设计和训练包含以下关键要素：

*   **长上下文扩展**：将RL的上下文窗口扩展至**128k**，研究发现随着上下文长度增加，模型性能持续提升。核心创新在于采用部分序列复用技术提升训练效率——通过重用已有序列的大部分内容来生成新序列，避免了完全重新生成的计算开销。研究表明，**上下文长度是LLM强化学习持续扩展的关键维度**。
*   **优化策略改进**：基于long-CoT提出了新的RL算法框架，并采用**online mirror descent变体**实现稳健的策略优化。通过引入高效的采样策略、长度控制机制和数据处理方案，进一步提升了算法性能。
*   **简化框架设计**：结合长上下文扩展和改进的策略优化方法，构建了一个简洁高效的LLM强化学习框架。通过扩展上下文长度，训练得到的CoT展现出规划、反思和纠正能力。增加的上下文长度相当于增加了搜索步骤数，实验证明无需借助蒙特卡罗树搜索、价值函数和过程奖励模型等复杂技术也能实现卓越性能。
*   **多模态能力**：模型通过文本和视觉数据的联合训练，获得了跨模态推理能力。

此外，研究团队提出了创新的**long2short**方法，利用long-CoT技术优化short-CoT模型，具体包括应用**long-CoT激活的长度控制**和**模型融合技术**。

方法：基于LLM的强化学习
-------------

Kimi k1.5的开发经历了多个阶段：预训练、基础**监督微调（SFT）**、long-CoT SFT和RL。

本章主要讨论RL相关内容，首先概述RL提示词集合的构建和long-CoT SFT，然后再深入探讨RL训练策略，最后介绍预训练和基础监督微调的详细内容。

### RL提示词集合构建

初步实验表明，RL提示词集合的质量和多样性对强化学习的有效性至关重要。精心构建的提示词集合不仅能引导模型进行可靠推理，还能有效降低**奖励黑客（reward hacking）**风险和避免**过度拟合表面特征**。

高质量RL提示词集合需具备三个关键特性：

*   **多样性覆盖**：提示词应涵盖STEM、编程和通用推理等多个领域，以提升模型的适应能力并确保其在不同领域的广泛应用。
*   **难度均衡**：提示词集合应包含难度分布合理的简单、中等和困难问题，促进模型的渐进学习，避免对特定难度水平的过度拟合。
*   **评估准确性**：提示词应便于验证器进行客观可靠的评估，确保模型性能评估基于正确的推理过程，而非表面特征或随机猜测。

为实现提示词集合的多样性覆盖，研究采用自动筛选机制选择需要复杂推理且易于评估的问题。数据集包含STEM领域、竞赛题目和通用推理任务等各类问题，同时整合了纯文本和图文混合的问答数据。研究团队开发了专门的标签系统，按领域和学科对提示词进行分类，确保各学科领域的均衡分布。

研究采用基于模型的方法，**利用模型自身能力动态评估提示词难度**。具体而言，对每个提示词，SFT模型在较高采样温度下生成十次答案，将答案正确率作为难度评估指标——正确率越低，难度越高。这种方法使难度评估与模型的内在能力保持一致，显著提升了RL训练效果。基于此方法，研究能够有效过滤大量简单样本，并在RL训练中灵活探索不同采样策略。

为防止reward hacking，研究确保每个提示词的推理过程和最终答案都能被准确验证。实践发现，部分复杂推理问题可能存在相对简单且易于猜测的答案，导致错误验证——即**模型通过错误推理得到正确答案**。为此，研究排除了选择题、判断题和证明题等容易产生此类问题的题型。

对于通用问答任务，研究提出了一种简单有效的方法识别和剔除易于作弊的提示词：让模型在不进行CoT推理的情况下直接猜测答案，如果在 NNN 次尝试内得到正确答案，则将该提示词判定为易于作弊并予以剔除。实验表明，**设置** N\=8N=8N=8 **可有效剔除大多数易于作弊的提示词**。开发更先进的验证模型仍是未来研究的重要方向。

### 长思维链监督微调（Long-CoT SFT）

基于优化后的RL提示词集合，研究团队运用提示词工程技术构建了一个规模精简但质量优异的long-CoT训练初始数据集，其中包含经过严格验证的文本和图像输入推理路径。这种方法虽与**拒绝采样（RS）**相似，但重点在于通过提示词工程生成long-CoT推理路径。

训练初始数据集设计融入了人类推理的核心认知要素：

*   **规划能力**：模型在执行前进行系统化步骤设计
*   **评估机制**：对中间步骤进行严谨的分析评估
*   **反思能力**：支持模型对方法进行审视和优化
*   **探索思维**：促进模型考虑多元解决方案

通过对该训练初始数据集进行轻量级SFT，模型成功掌握了这些推理策略。经过微调的long-CoT模型在生成详实且逻辑严密的响应方面表现出显著提升，从而增强了其在多样化推理任务中的整体性能。

### 强化学习（RL）

**强化学习问题设定**

给定训练数据集 D\={(xi,yi∗)}in\=1 D = \\{(xi, y^∗\_i )\\}^n\_i=1 D = \\{(xi, y^∗\_i )\\}^n\_i=1，包含问题xi和对应的真实答案 yi∗y^∗\_i y^∗\_i ，目标是训练策略模型 πθ\\pi\_{\\theta}\\pi\_{\\theta} 准确求解测试问题。对于复杂推理问题，从问题 xxx 到解决方案 yyy 的映射并非简单直接。为此，CoT方法提出使用中间步骤序列 z\=(z1,z2,...,zm)z = (z\_1, z\_2, ..., z\_m)z = (z\_1, z\_2, ..., z\_m) 连接 xxx 和 yyy ，其中每个 ziz\_iz\_i 是一个有意义的中间推理步骤。

在求解问题 xxx 时，模型通过自回归采样产生思维步骤 zt∼πθ(·|x,z1,...,zt−1)z\_t ∼ \\pi\_{\\theta}(·|x, z\_1, ..., z\_{t-1})z\_t ∼ \\pi\_{\\theta}(·|x, z\_1, ..., z\_{t-1}) ，最终得到答案 y∼πθ(·|x,z1,...,zm)y ∼ \\pi\_{\\theta}(·|x, z\_1, ..., z\_m)y ∼ \\pi\_{\\theta}(·|x, z\_1, ..., z\_m) 。此过程简记为 y,z∼πθy,z ∼ \\pi\_{\\theta}y,z ∼ \\pi\_{\\theta}。

为增强模型推理能力，规划算法在推理时探索多种思维路径，生成改进的CoT。核心思想是**构建由价值估计引导的思维搜索树**。搜索树 T\\mathcal{T}\\mathcal{T} 中每个节点表示部分解 s\=(x,z1:|s|)s = (x, z\_{1:|s|})s = (x, z\_{1:|s|}) ，包含问题 xxx 和思维序列 z1:|s|z\_{1:|s|}z\_{1:|s|} 。规划算法使用评价模型 vvv 提供反馈 v(x,z1:|s|)v(x, z\_{1:|s|})v(x, z\_{1:|s|}) ，可以是判别分数或语言序列。

从算法角度，规划算法 A\\mathcal{A}\\mathcal{A} 在第 ttt 次迭代基于历史搜索 (s1,v(s1),...,st−1,v(st−1))(s\_1, v(s\_1), ..., s\_{t-1}, v(s\_{t-1}))(s\_1, v(s\_1), ..., s\_{t-1}, v(s\_{t-1})) 确定下一个搜索方向。为简化表示，用 zzz 替代 sss 和 vvv ，将规划算法视为直接作用于推理步骤序列的映射 A(·|z1,z2,...)\\mathcal{A}(·|z\_1, z\_2, ...)\\mathcal{A}(·|z\_1, z\_2, ...) 。

这个方法提供了生成高质量CoT的新视角：**无需显式构建搜索树，而是训练模型近似此过程**。思维数量（即语言token）类似于传统规划算法的计算预算。长上下文窗口的进展使训练和测试阶段都能无缝扩展。

因此，研究团队采用RL训练模型生成CoT。奖励模型 rrr 根据真实答案 yyy 评估预测答案 y∗y^\*y^\* 的正确性_，_赋值r(x,y,y∗)∈{0,1}r(x, y, y\*) \\in \\{0,1\\}r(x, y, y\*) \\in \\{0,1\\} 。对可验证问题，奖励由预定义标准或规则直接确定。例如，在编程问题中，研究团队评估答案是否通过测试用例。对具有自由形式真实答案的问题，团队训练奖励模型 r(x,y,y∗)r(x, y, y\*)r(x, y, y\*) 预测答案是否匹配真实答案。给定问题 xxx ，模型 πθ \\pi\_{\\theta} \\pi\_{\\theta} 通过采样过程 z∼πθ(·|x)z\\sim\\pi\_{\\theta}(·|x) z\\sim\\pi\_{\\theta}(·|x) ，y∼πθ(·|x,z)y\\sim\\pi\_{\\theta}(·|x,z) y\\sim\\pi\_{\\theta}(·|x,z) 生成CoT和最终答案。生成的CoT质量由其是否能导向正确的最终答案来评估。

总结起来，策略优化目标为：

![](https://picx.zhimg.com/v2-bedbf9a4261422946037d844c23f8abf_r.jpg)

通过扩展RL训练，目标是训练一个结合简单提示词CoT和规划增强CoT优势的模型。模型在推理时仍使用自回归采样，避免了部署复杂的并行规划算法。

关键区别在于，**模型不仅执行推理步骤，还要学习错误识别、回溯和解决方案优化等关键规划技能**。

**策略优化**

研究采用**[在线策略镜像下降](https://zhida.zhihu.com/search?content_id=252988821&content_type=Article&match_order=1&q=%E5%9C%A8%E7%BA%BF%E7%AD%96%E7%95%A5%E9%95%9C%E5%83%8F%E4%B8%8B%E9%99%8D&zhida_source=entity)（online policy mirror descent）**算法的变体作为训练算法。算法通过迭代方式执行。在第 iii 次迭代中，将当前模型 πθi\\pi\_{\\theta\_i}\\pi\_{\\theta\_i} 作为参考模型，优化如下相对熵正则化的策略优化问题：

![](https://picx.zhimg.com/v2-deb1d062e2b8589172502d21b2fc2873_r.jpg)

其中 τ\>0\\tau>0\\tau>0 为控制正则化程度的参数。该目标函数具有闭合形式解：

![](https://pica.zhimg.com/v2-0db8d253934a4505e86e69b7dad7b90a_r.jpg)

这里 Z\=∑y′,z′πθi(y′,z′|x)exp(r(x,y′,y∗)/τ)Z = \\sum\_{}^{}{\_{y',z'}} \\pi\_{\\theta\_i}(y', z'|x)\\text{exp}(r(x, y', y^\*)/\\tau)Z = \\sum\_{}^{}{\_{y',z'}} \\pi\_{\\theta\_i}(y', z'|x)\\text{exp}(r(x, y', y^\*)/\\tau) 为归一化因子。

对两边取对数得到以下约束条件，这样能够在优化过程中利用**离线策略（off-policy）**数据：

![](https://pica.zhimg.com/v2-605967989222c20d31eb1b786d01ede4_r.jpg)

基于这一约束关系，可以建立如下替代损失函数：

![](https://pic1.zhimg.com/v2-a1a6aebf3c6ab7a0e869cc50888994b0_r.jpg)

为近似 τlogZ\\tau \\text{log} Z\\tau \\text{log} Z ，采用样本 (y1,z1),...,(yk,zk)∼πθi(y\_1, z\_1), ..., (y\_k, z\_k) ∼ \\pi\_{\\theta\_i}(y\_1, z\_1), ..., (y\_k, z\_k) ∼ \\pi\_{\\theta\_i}:

τlog⁡Z≈τlog⁡1k∑j\=1kexp⁡(r(x,yj,y∗)/τ)\\tau \\log Z \\approx \\tau \\log \\frac{1}{k}\\sum\_{j=1}^k \\exp(r(x, y\_j, y^\*)/\\tau)\\tau \\log Z \\approx \\tau \\log \\frac{1}{k}\\sum\_{j=1}^k \\exp(r(x, y\_j, y^\*)/\\tau)

实践表明，使用采样奖励的经验均值 r¯\=mean(r(x,y1,y∗),...,r(x,yk,y∗))\\bar{r}= \\text{mean}(r(x, y\_1, y^\*), ..., r(x, y\_k, y^\*))\\bar{r}= \\text{mean}(r(x, y\_1, y^\*), ..., r(x, y\_k, y^\*)) 能够获得有效结果。这是合理的，因为当 τ\\tau\\tau 趋向无穷时， τlog⁡Z\\tau \\log Z\\tau \\log Z 趋近于 πθi\\pi\_{\\theta\_i}\\pi\_{\\theta\_i} 下的期望奖励。

对每个问题 xxx ，使用参考策略 πθi\\pi\_{\\theta\_i}\\pi\_{\\theta\_i} 采样 kkk 个响应，最终的梯度形式为：

![](https://pic3.zhimg.com/v2-7e4b188f2145961620a6fa38dffb33ea_r.jpg)

对熟悉策略梯度方法的研究者而言，这个梯度形式类似于使用采样奖励均值作为基准的策略梯度。主要区别在于：响应来自 πθi\\pi\_{\\theta\_i}\\pi\_{\\theta\_i} 而非**在线策略（on-policy）**，并引入了L2正则化。因此可将其视为常规在线正则化策略梯度算法向off-policy情况的自然扩展。从训练集 DDD 中采样一批问题并更新参数至 θi+1\\theta\_{i+1}\\theta\_{i+1} ，作为下一轮迭代的参考策略。由于参考策略的变化导致每轮迭代面对不同的优化问题，团队在每轮迭代开始时重置优化器。

研究团队选择在训练系统中排除价值网络，这种做法在先前研究中也有应用。这不仅显著提升了训练效率，他们也推测传统RL中使用价值函数进行信用分配可能不适合当前场景。

考虑如下情况：模型已生成部分CoT序列 (z1,z2,...,zt)(z\_1, z\_2, ..., z\_t)(z\_1, z\_2, ..., z\_t) ，面临两个可能的下一步推理： zt+1z\_{t+1}z\_{t+1} 和 zt+1′z'\_{t+1}z'\_{t+1} 。假设 zt+1z\_{t+1}z\_{t+1} 直接导向正确答案，而 zt+1′z'\_{t+1}z'\_{t+1} 包含错误。若存在理想的价值函数，它会显示 zt+1z\_{t+1}z\_{t+1} 比 zt+1′z'\_{t+1}z'\_{t+1} 具有更高价值。根据标准信用分配原则，**选择** zt+1′z'\_{t+1}z'\_{t+1} **会因相对当前策略具有负优势而受到惩罚**。

然而，探索 zt+1′z'\_{t+1}z'\_{t+1} 对训练模型生成long-CoT具有重要价值。通过使用基于long-CoT的最终答案判断作为奖励信号，只要模型能够成功纠正并达到正确答案，就能从选择 zt+1′z'\_{t+1}z'\_{t+1} 中学习试错模式。这个例子的关键启示是：**应当鼓励模型探索多样化推理路径，以增强其解决复杂问题的能力**。这种探索方法产生了丰富的经验，支持关键规划技能的发展。

研究的主要目标不局限于提高训练问题的准确率，而是着重于使模型掌握有效的问题解决策略，最终提升其在测试问题上的表现。

**长度惩罚机制**

研究发现模型在RL训练过程中出现overthinking现象，导致响应长度显著增加。尽管这提升了模型性能，但过长的推理过程不仅增加了训练和推理成本，也不符合人类使用偏好。为此，研究引入长度惩罚机制来控制token长度的快速增长，提高模型的token使用效率。

**长度惩罚计算方法** 对于问题 xxx 的 kkk 个采样响应 (y1,z1),...,(yk,zk)(y\_1, z\_1),...,(y\_k, z\_k)(y\_1, z\_1),...,(y\_k, z\_k) 和真实答案 y∗y^\*y^\* ，定义：

*   len(i)\\text{len(i)}\\text{len(i)} ：表示 (yi,zi)(y\_i, z\_i)(y\_i, z\_i) 的长度
*   min\_len\\text{min\_len}\\text{min\_len} ：所有响应中的最小长度
*   max\_len\\text{max\_len}\\text{max\_len} ：所有响应中的最大长度

当 max\_len\\text{max\_len}\\text{max\_len} = min\_len\\text{min\_len}\\text{min\_len} 时，所有响应的长度奖励均为零。其他情况下，长度奖励计算如下：

![](https://picx.zhimg.com/v2-7a72d13e1125ad72a17cdbaf73c08a6b_r.jpg)

其中：λ\=0.5−len(i)−min\_lenmax\_len−min\_lenλ = 0.5 - \\frac{len(i) - min\\\_len}{max\\\_len - min\\\_len}λ = 0.5 - \\frac{len(i) - min\\\_len}{max\\\_len - min\\\_len}

这种机制的核心思想是：对于正确答案，鼓励较短响应并抑制较长响应；对于错误答案，则明确惩罚冗长响应。该长度奖励通过权重系数与原始奖励相结合。

**采样策略优化**

尽管RL算法在采样方面表现出良好特性（复杂问题能够提供更大的梯度信息），但其训练效率仍存在限制。研究表明，合理设计的优先采样方法可能带来更显著的性能提升。基于这一认识，研究团队利用多维度信号改进采样策略：

1.  利用训练数据的自然难度分布，如数学竞赛题目相比基础数学题目具有更高难度
2.  通过RL训练过程中对同一问题的多次采样，跟踪记录每个问题的成功率作为难度衡量指标

**课程采样** 研究团队采用由易到难的渐进式训练策略。考虑到初始RL模型性能有限，将有限的计算资源用于极具挑战性的问题往往只能获得少量正确样本，降低了训练效率。研究发现，已收集的训练数据自然包含年级和难度标签，这使得基于难度的采样策略成为提升训练效率的有效方法。

**优先级采样** 作为课程采样的补充，研究采用优先级采样策略重点关注模型性能欠佳的问题。通过追踪每个问题i的成功率 sis\_is\_i ，按照 1−si1-s\_i1-s\_i 的比例进行采样，使成功率较低的问题获得更高的采样概率。这种方法引导模型着重改进其薄弱环节，加快学习速度并提升整体性能。

**训练方案补充细节**

**编程Test Case生成** 鉴于网络上的编程问题普遍缺乏test case，研究设计了自动生成test case的方法作为RL训练的奖励信号。研究主要针对不需要特殊评判的问题，并假设这些问题具有可用的标准解答，以便利用这些解答生成高质量test case。

研究采用测试用例生成库**CYaRon**增强测试方案。基于Kimi k1.5基础模型，结合CYaRon的usage statement和问题描述生成test case。

具体流程为：首先为每个问题生成50个test case，并为每个test case随机抽取10个ground truth submissions进行验证。当至少**7/10**个子任务产生一致结果时，该test case被认定为有效。通过筛选获得的test case集合需确保至少**9/10**个子任务全部通过才被纳入训练集。

数据统计显示，在1,000个在线竞赛问题中，约614个无需特殊评判。研究团队开发了463个test case生成器，每个至少产生40个有效test case，最终将323个问题纳入训练集。

**数学问题奖励建模** 评估数学解答面临的主要挑战是答案表达形式的多样性，如 a2−4a^2-4a^2-4 和 (a+2)(a−2)(a+2)(a-2)(a+2)(a-2) 可能都是同一问题的有效解答。为提高奖励模型的评分准确性，研究采用两种方法：

1.  **经典奖励模型（Classic RM）** 基于InstructGPT方法论，实现了value-head奖励模型，并收集约80万数据点进行微调。模型接收"问题"、"参考答案"和"响应"作为输入，输出表示正确性的标量值。
2.  **Chain-of-Thought奖励模型（CoT-RM）** 最新研究表明，结合CoT推理的奖励模型在需要精细正确性判断的任务（如数学）中表现显著优于传统方法。研究团队收集了约80万个CoT标注样本用于Kimi模型微调。该方法在保持相同输入的基础上，通过显式生成分步推理过程，最终以JSON格式输出正确性判断，提供更可靠且可解释的奖励信号。

手动评估显示，Classic RM准确率达84.4%，而**CoT-RM**准确率达**98.5%**。为确保反馈质量，RL训练采用CoT-RM。

**视觉数据构建**

为增强模型的实际图像推理能力并优化视觉输入与LLM的对齐效果，视觉RL数据来源包括三个主要类别：真实世界数据、合成视觉推理数据和文本渲染数据。

1.  **真实世界数据** 涵盖需要图形理解和推理的各级科学题目、基于视觉感知的位置推断任务、复杂图表解析等场景。这类数据增强了模型在实际场景中的视觉推理能力。
2.  **合成视觉推理数据** 包含程序化生成的图像和场景，专注于提升空间关系理解、几何模式识别和物体交互等特定视觉推理能力。这类数据提供了可控的测试环境和持续的训练样本供应。
3.  **文本渲染数据** 通过将文本内容转换为视觉格式，确保模型在跨模态文本查询中保持一致性。包括文档、代码片段和结构化数据的图像转换，提升了模型对文本密集型图像的处理能力。

这三类数据的整合对构建综合性视觉语言模型至关重要，确保模型能在不同输入模态下保持稳定性能，有效应对各类实际应用场景。

### Long2short：面向Short-CoT模型的上下文压缩

尽管long-CoT模型展现出优异性能，但相比标准short-CoT LLM，其在**测试阶段消耗更多token**。研究发现，可以将long-CoT模型的推理模式迁移至short-CoT模型，在token预算有限的约束下提升模型性能。研究提出多种解决long2short问题的方法，包括**模型合并、最短拒绝采样、DPO**和**long2short RL**。具体实现如下：

**模型合并** 研究发现模型合并不仅有助于保持泛化能力，在提升token效率方面也表现出效果。该方法通过简单平均long-cot与short-cot模型的权重，无需额外训练即可获得性能提升。

**最短拒绝采样** 基于模型对同一问题生成的响应长度具有较大变异性的观察，研究设计了最短拒绝采样方法。该方法对每个问题进行 nnn 次采样（实验中 n\=8n=8n=8 ），选取最短的正确响应用于监督微调。

**DPO** 采用与最短拒绝采样相似的思路，研究使用Long-CoT模型生成多个响应样本。从中选取最短正确解答作为正样本，将其他较长响应（包括错误答案和长度超过正样本1.5倍的正确答案）作为负样本。这些正负样本对构成对偏好数据，用于DPO训练。

**Long2short RL** 在完成标准RL训练后，研究选择一个在性能和token效率之间达到最佳平衡的模型作为基础模型，进行专门的long2short RL训练。第二阶段引入长度惩罚机制，显著降低最大展开长度，以加强对超长响应的惩罚（即使答案正确）。

### 训练细节补充

**预训练阶段**

Kimi k1.5基础模型采用多样化、高质量的多模态语料库进行训练。语言数据涵盖英语、中文、代码、数学推理和知识五大领域。多模态训练数据包括图像描述、图文交错、OCR、知识库和QA数据集，构建模型的视觉-语言能力。研究采用严格的质量控制确保预训练数据集在相关性、多样性和平衡性方面达到要求。

预训练过程分为三个阶段：

1.  **视觉-语言预训练**：建立强大的语言基础模型，随后逐步整合多模态能力
2.  **Cooldown阶段**：使用精选和合成数据强化模型能力，重点关注推理和知识型任务
3.  **长上下文激活**：将序列处理能力扩展至128K token

详细的预训练方案请参见原文附录B。

**基础监督微调**

研究团队构建了多领域的基础SFT数据集。对于问答、写作等非推理任务，首先通过人工标注建立种子数据集并训练种子模型。随后收集多样化提示词，利用种子模型生成多个响应，由标注人员排序并优化最优响应。对于数学和编程等推理任务，采用基于规则和奖励建模的验证方法，通过拒绝采样扩充数据集。

基础SFT数据集规模约100万文本样本，包括：

*   通用问答：50万样本
*   编程：20万样本
*   数学和科学：20万样本
*   创意写作：5千样本
*   长上下文任务：2万样本

另构建100万文本-视觉样本，涵盖图表解释、OCR、图像对话、视觉编程、视觉推理及视觉辅助的数学科学问题。

训练分两阶段：

1.  32k token序列长度：1个epoch，学习率 2×10−52\\times10^{-5}2\\times10^{-5} 降至 2×10−62\\times10^{-6}2\\times10^{-6}
2.  128k token序列长度：1个epoch，学习率从 1×10−51\\times10^{-5}1\\times10^{-5} 降至 1×10−61\\times10^{-6}1\\times10^{-6}

为优化训练效率，采用多样本序列打包策略。

### RL基础设施

**LLM大规模强化学习训练系统**

在人工智能领域中，RL已经成为LLM的一个关键训练方法。这种方法源于其在复杂游戏领域取得的成功，如通过AlphaGo、AlphaStar和OpenAI Dota Five系统在围棋、星际争霸II和Dota 2等游戏中的应用。基于这些成功经验，Kimi k1.5系统采用了迭代同步RL框架，通过持续学习和适应来提升模型的推理能力。该系统的一项关键创新在于引入了Partial Rollout技术，用于优化复杂推理轨迹的处理。

![](https://pic3.zhimg.com/v2-033be85e0a15cbe8bfb970bfe36eee3e_r.jpg)

图3：LLM大规模强化学习训练系统

如图3(a)所示，该RL训练系统采用迭代同步方式运行，每次迭代包含rollout阶段和训练阶段。在rollout阶段，中央主控协调的rollout工作器通过与模型交互生成轨迹，产生对各类输入的响应序列。这些轨迹随后存储在replay buffer中，通过打破时间相关性来确保训练数据集的多样性和无偏性。在训练阶段，训练工作器利用这些经验更新模型权重。通过这种循环过程，模型能够持续从行为中学习，并不断调整策略以提升性能。

中央主控作为核心协调者，负责管理rollout工作器、训练工作器、奖励模型评估与replay buffer之间的数据流动和通信。它确保系统高效运行，平衡负载并实现高效的数据处理。

训练工作器会处理这些rollout轨迹（无论是单次迭代完成还是跨多次迭代），计算梯度更新以优化模型参数并提升性能。奖励模型负责监督这一过程，评估模型输出质量并提供反馈指导训练。奖励模型的评估对于确定模型策略的有效性和优化模型性能起着关键作用。

此外，系统还集成了专门的代码执行服务，作为奖励模型的重要组成部分，用于处理代码相关问题。该服务在实际编程场景中评估模型输出，确保模型学习与实际编程挑战保持一致。通过实际代码执行来验证模型解决方案，这一反馈循环对于优化模型策略和提升其在代码相关任务中的表现至关重要。

**Long-CoT RL的部分展开（Partial Rollout）技术**

该研究的核心思想之一是实现长上下文RL训练的扩展。部分展开技术是一项重要创新，通过管理长短轨迹的展开过程，有效解决了处理长CoT特征的挑战。该技术设定了固定的输出token预算，对每个展开轨迹的长度进行限制。

当轨迹在展开阶段超出token限制时，未完成部分会存入replay缓存并在下一轮迭代中继续处理。这确保了系统资源不会被单个长轨迹占用。由于展开工作器采用异步操作，部分工作器处理长轨迹时，其他工作器可以同时处理新的短轨迹任务。这种异步机制确保所有展开工作器持续参与训练过程，最大化计算效率，从而优化系统整体性能。

如图3(b)所示，Partial Rollout系统将长响应在多个迭代间（从iter n-m到iter n）分段处理。Replay Buffer作为中央存储机制维护这些响应片段，其中仅当前迭代（iter n）需要进行在线策略计算。先前的片段（iter n-m到n-1）可直接从缓存中重用，避免重复展开。这种分段方法显著降低计算开销：系统通过增量方式处理和存储片段，而非一次性处理整个响应，既保持了快速迭代速度，又支持更长响应的生成。在训练过程中，可以将特定片段排除在损失计算之外，进一步优化学习过程，使系统既高效又具有扩展性。

部分展开的实现还包含重复检测功能。系统能够识别生成内容中的重复序列并及时终止，在保证输出质量的同时减少不必要的计算。对检测到的重复内容施加额外惩罚，有效抑制提示词中的冗余内容生成。

**训练和推理的混合部署**

RL训练过程包含以下阶段：

*   **训练阶段**：首先，**[Megatron](https://zhida.zhihu.com/search?content_id=252988821&content_type=Article&match_order=1&q=Megatron&zhida_source=entity)**和**[vLLM](https://zhida.zhihu.com/search?content_id=252988821&content_type=Article&match_order=1&q=vLLM&zhida_source=entity)**在独立容器中运行，由**checkpoint-engine**进程封装。Megatron启动训练流程，完成后卸载GPU内存并准备向vLLM传输当前权重。
*   **推理阶段**：Megatron卸载后，vLLM以临时模型权重启动，并通过**Mooncake**更新从Megatron传输的最新权重。展开完成后，checkpoint-engine终止所有vLLM进程。
*   **后续训练阶段**：释放vLLM占用的内存后，Megatron重新加载内存并启动新一轮训练。

现有工作难以同时支持以下特性：

*   **复杂并行策略**：Megatron与vLLM可能采用不同的并行策略，使得Megatron在多节点分布的训练权重难以与vLLM共享。
*   **最小化空闲GPU资源**：在On-Policy RL中，**SGLang**和vLLM等最新工作可能在训练过程中占用GPU，导致训练GPU闲置。训练和推理共享设备将提高效率。
*   **动态扩展能力**：某些情况下，增加推理节点数量而保持训练过程不变可显著提升速度。该系统能够根据需要高效利用空闲GPU节点。

如图4所示，该混合部署框架基于Megatron和vLLM实现，训练到推理阶段转换时间不超过1分钟，反向转换仅需约10秒。

![](https://pic4.zhimg.com/v2-a8c55efbcbcff3ae8178217296edb9d5_r.jpg)

图4：混合部署框架

混合部署策略利用**Kubernetes Sidecar**容器共享所有可用GPU，将训练和推理工作负载整合到同一个pod中。主要优势包括：

*   实现高效的资源共享和管理，避免独立节点部署时训练节点等待推理节点而闲置
*   通过不同部署镜像，使训练和推理能够独立迭代以优化性能
*   架构支持灵活集成其他框架，不局限于vLLM

Checkpoint Engine负责管理vLLM进程生命周期，提供HTTP API以触发vLLM操作。系统采用etcd服务管理的全局元数据系统来广播操作和状态，确保整体一致性和可靠性。

由于**CUDA图**、**NCCL缓冲区**和**NVIDIA驱动程序**的影响，vLLM完全释放GPU内存存在挑战。为最小化对vLLM的修改，系统采用按需终止和重启策略，提高GPU利用率和容错能力。

Megatron中的工作器将checkpoint转换为共享内存中的Hugging Face格式，同时考虑**流水线并行（PP）**和**专家并行（EP）**，仅保留**张量并行（TP）**。共享内存中的checkpoint随后分片并在全局元数据系统注册。系统通过Mooncake使用**RDMA**在节点间传输checkpoint，并对vLLM进行必要修改以支持权重文件加载和TP转换。

**代码沙盒（Code Sandbox）**

该研究开发了sandbox作为执行用户提交代码的安全环境，专门针对代码执行和基准评估进行优化。sandbox通过动态切换容器镜像，支持**MultiPL-E、DMOJ Judge Server 2、Lean、Jupyter Notebook**等不同场景应用。

在编码任务的RL训练中，sandbox通过提供一致且可重复的评估机制确保训练数据判断可靠性。其反馈系统支持代码执行反馈和仓库级编辑等多阶段评估，同时保持统一上下文，确保不同编程语言间的公平基准比较。

服务部署在Kubernetes上以实现可扩展性和弹性，并通过HTTP端点提供外部集成。Kubernetes的自动重启和滚动更新功能保证了系统可用性和容错能力。

为优化性能并支持RL环境，代码执行服务采用以下技术提升效率、速度和可靠性：

*   **使用Crun**：采用**crun**替代Docker作为容器运行时，显著降低容器启动时间；
*   **Cgroup重用**：预创建容器使用的**cgroup**，解决高并发场景下cgroup频繁创建销毁造成的性能瓶颈；
*   **磁盘使用优化**：采用上层挂载为**tmpfs**的覆盖文件系统控制磁盘写入，提供固定大小的高速存储空间，适用于临时工作负载。

![](https://pic3.zhimg.com/v2-763805358a3f0d79661b007db6230098_r.jpg)

这些优化提升了代码执行中RL的效率，为评估RL生成代码提供稳定可靠的环境，对迭代训练和模型优化起到关键作用。

实验
--

### 评估

k1.5作为多模态模型，在不同模态的多个基准测试中进行了全面评估，详细评估设置参见原文附录C。基准测试主要分为三类：

*   **文本基准**：MMLU、IF-Eval、CLUEWSC、C-EVAL；
*   **推理基准**：HumanEval-Mul、LiveCodeBench、Codeforces、AIME 2024、MATH-500；
*   **视觉基准**：MMMU、MATH-Vision、MathVista。

### 主要结果

**K1.5 long-CoT模型**：表2展示了Kimi k1.5 long-CoT模型的性能。通过long-CoT SFT和视觉-文本联合RL，模型的长期推理能力获得显著提升。测试阶段的计算扩展进一步增强了模型性能，使其在各模态任务中达到SOTA水平。评估结果表明，**模型在长上下文的推理、理解和信息综合能力方面有显著提升，推动了多模态AI技术的发展**。

![](https://pic3.zhimg.com/v2-0a41cecb0e44d436cfb4d6574709220a_r.jpg)

表2：Kimi k1.5 long-CoT与主流开源及专有模型的性能对比

**K1.5 short-CoT模型**：表3展示了Kimi k1.5 short-CoT模型的性能。该模型集成了传统SFT、RL和long-to-short distillation等技术。结果显示，**k1.5 short-CoT模型在文本、视觉和推理等多项任务中达到或超越了领先开源和专有模型的性能水平，尤其在自然语言理解、数学、编码和逻辑推理方面表现突出**。

![](https://pic1.zhimg.com/v2-461a594ac85b550ec0ca924c984b4bd6_r.jpg)

表3：Kimi k1.5 short-CoT与主流开源及专有模型的性能对比（VLM模型性能数据来源于OpenCompass基准平台：https://opencompass.org.cn/）

### 长上下文扩展

研究采用中等规模模型研究LLM的RL扩展特性。图5展示了小型模型在数学提示词集上训练时，训练准确率和响应长度的演变过程。

![](https://pic4.zhimg.com/v2-a550c1e1ff04d925858ef47fcac94827_r.jpg)

图5：训练准确率和长度随迭代增长的变化。数据来自规模远小于k1.5 long-CoT的内部long-cot模型。阴影区表示响应长度的95%置信区间。

随训练进行，响应长度和性能准确率同步提升。更具挑战性的基准测试展现出更陡峭的响应长度增长曲线，表明模型能为复杂问题生成更详细的解决方案。图6显示模型的输出上下文长度与问题解决能力呈现强相关性。k1.5最终版本支持128k上下文长度，并在复杂推理基准测试中持续改进。

![](https://pic3.zhimg.com/v2-75f464e968166efe44b5f83c7a17e2e4_r.jpg)

图6：模型性能与响应长度的正相关关系。

### Long2short

研究对比了新提出的long2short RL算法与之前介绍的DPO、最短拒绝采样和模型合并方法，重点关注long2short问题的token效率，特别是long-cot模型对短模型的增益效果。图7中：

*   **k1.5-long**：用于long2short训练的long-cot模型
*   **k1.5-short w/ rl**：使用long2short RL训练的短模型
*   **k1.5-short w/ dpo**：通过DPO训练优化token效率的短模型
*   **k1.5-short w/ merge**：模型合并后的结果
*   **k1.5-short w/ merge + rs**：合并模型应用最短拒绝采样后的短模型
*   **k1.5-shortest**：long2short训练中获得的最短模型

![](https://pic1.zhimg.com/v2-29912a0ab64a45751f2f88894ca039c0_r.jpg)

图7：Long2Short性能对比。k1.5系列展示出更优的token效率。

如图7所示，**long2short RL算法展现出优于DPO和模型合并等方法的token效率**。k1.5系列模型（橙色）整体上较其他模型（蓝色）具有更优的token效率。例如，k1.5-short w/ rl在AIME2024上达到60.8的Pass@1分数（8次运行平均），仅使用平均3,272个token。k1.5-shortest在MATH500上获得88.2的Pass@1分数，token消耗与其他短模型相当。

### 消融研究

**模型规模和上下文长度扩展**：研究主要通过RL增强模型生成扩展CoT的能力来提升推理能力。为验证该方法相比简单增加模型规模的优势，研究使用相同数据集训练了两个不同规模模型，记录RL训练过程中所有检查点的评估结果和平均推理长度。

![](https://pic1.zhimg.com/v2-2691b25ec171fa253f82c105081df31a_r.jpg)

图8：模型规模、性能与响应长度关系

结果如图8所示：**较大模型虽然初始表现更优，但较小模型通过RL优化的长CoT能达到相当性能水平**。较大模型的token效率整体更高，表明在追求最佳性能时，扩展大模型上下文长度具有更高上限且更具token效率。但在测试计算资源受限情况下，训练具有更大上下文长度的小模型也是可行方案。

**负梯度效果**：研究分析了ReST作为策略优化算法的效果。ReST与其他RL方法的主要区别在于：它通过拟合当前模型的最佳采样响应来迭代优化，而不使用负梯度惩罚错误响应。

![](https://pic4.zhimg.com/v2-9f51f6c95c3f5754f53b1d61a117bcab_r.jpg)

图10：使用ReST进行策略优化的效果对比

如图10所示，该方法展现出优于ReST的样本复杂度，证明负梯度显著提升了长CoT生成效率。方法不仅提升推理质量，还优化训练过程，以更少样本实现稳定性能。这表明策略优化算法选择的关键性，因为ReST与其他RL方法的性能差异在本场景比其他领域更显著。

**采样策略**：研究验证了本文前面提出的课程采样策略有效性。训练数据集 \\mathcal{D}\\mathcal{D} 包含不同难度的多样化问题。课程采样方法先用 \\mathcal{D}\\mathcal{D} 进行warm-up，再聚焦困难问题训练，相比无课程调整的均匀采样基准方法表现更优。

![](https://pic2.zhimg.com/v2-504e7541ee755b71fcfeff18b5b2dcdf_r.jpg)

图9：课程学习对模型性能的影响

如图9所示，课程采样显著提升了模型性能。通过逐步增加难度挑战模型，使其形成更稳健的理解能力，特别是在初始通用训练后集中训练困难问题，有效增强了模型的推理和解题能力。

小结
--

研究介绍了k1.5（基于RL训练的最新多模态LLM）的训练方法和系统设计。实践表明，上下文长度扩展是LLM持续改进的关键因素。研究采用优化的学习算法和基础设施优化（如部分展开）实现高效的长上下文RL训练。提高长上下文RL训练效率和可扩展性仍是重要的研究方向。

本研究在策略优化方面的主要贡献是多项技术的创新组合。具体包括：为LLM设计long-CoT RL框架，推导在线镜像下降的变体实现稳健优化，以及优化采样策略、长度惩罚和数据配方以提升RL性能。

研究证明，仅通过长上下文扩展和改进的策略优化，无需使用MCTS、价值函数和PRM等复杂技术，即可实现卓越性能。

研究还发现long2short方法具有显著潜力，能有效提升短CoT模型性能。通过将long2short方法与long-CoT RL迭代结合，可进一步优化token效率，在有限上下文长度约束下实现最优性能。

团队后续研究方向包括：**改进信用分配机制，在保持模型探索能力的同时减少过度思考问题**。

* * *

**别着急走，下面还有**

附录B：预训练
-------

RL效率与基础模型性能密切相关。Gemini和Llama等前沿模型证明了预训练数据质量的关键作用。鉴于目前开源模型普遍缺乏数据处理流程的完整透明度，该研究详细介绍数据流程和方法，重点阐述多模态预训练数据方案。

### 语言数据

预训练语料库涵盖英语、中文、代码、数学推理和知识五大领域，每个领域采用严格的质量控制机制，通过系统评估确保数据质量和有效性。

**英语和中文文本**采用多维质量过滤框架：

*   规则过滤：移除重复内容、机器翻译文本和低质量网页抓取
*   FastText分类：基于语言特征和语义连贯性识别内容质量
*   向量相似度分析：通过文档向量计算去重，保留语义变体
*   LLM质量评估：评估文档连贯性、信息量和教育价值

**代码数据**分为两类处理：

*   纯代码：采用BigCode方法，去除冗余语言，平衡32种主要编程语言的分布
*   文本-代码混合：使用向量方法筛选高质量数据

**数学推理数据**从公开网络资源获取，采用专门的数据处理流程：

*   开发数学领域专用OCR模型和清理程序
*   采用FastText初筛和语言模型精筛的两阶段清理

**知识数据**聚焦学术内容，包括：

*   来源：学术练习、教材、研究论文等
*   处理：专用OCR处理，多维度标注（质量、教育价值、类型）
*   筛选：基于OCR质量阈值和教育价值评分系统
*   采样：根据实验确定不同文档类型的采样策略，平衡知识获取和泛化能力

### 多模态数据

预训练语料库旨在提供高质量数据，使模型能处理和理解包括文本、图像和视频在内的多种模态信息。语料库包含五类精选数据：**描述数据、图文交错数据、OCR数据、知识数据**和**通用问答数据**。

数据处理流程包括**过滤、合成**和**去重**，确保数据质量。有效的多模态数据策略在视觉和语言联合训练中至关重要，既保持语言模型能力，又促进跨模态知识对齐。

主要数据类型包括：

**描述数据**：提供基本模态对齐和广泛世界知识。整合开源中英文数据集和内部数据，严格限制合成描述数据比例，避免因缺乏真实世界知识导致幻觉。采用严格质量控制流程避免重复，保持高图文相关性。

**图文交错数据**：预训练阶段有多方面益处，如提升多图理解能力、提供详细知识、增强长多模态上下文学习能力。整合开源数据集和内部数据，除标准质量控制外，增加数据重排序确保图文正确顺序。

**OCR数据**：k1.5中OCR能力对模型与人类价值观对齐至关重要。数据来源多样，包括开源和内部数据集，覆盖多语种文本、密集文本布局、网络内容和手写样本。

**知识数据**：构建全面的人类知识库。标准分类法平衡各类内容，确保数据源多样性。使用布局解析器和OCR模型处理内容。针对网络素材中的信息图，开发额外流程捕获图像中的纯文本信息。

**通用QA数据**：包含高质量学术数据集，涉及定位、表格/图表问答、网络智能体和通用QA。通过评分模型和人工分类保持难度和多样性平衡。

### 模型架构

Kimi k系列模型采用改进的Transformer解码器，集成多模态能力和架构优化策略（图11）。扩展实验表明，基础模型性能主要来自预训练数据质量和多样性的改进。具体架构扩展实验细节将在未来发表。

![](https://pic1.zhimg.com/v2-08f5659eedfe8484b68efb8a2930b9e2_r.jpg)

图11：Kimi k1.5支持图文交错输入，通过大规模强化学习增强推理能力。

训练阶段：

*   **视觉-语言预训练阶段**：先仅训练语言数据建立语言模型基础，再逐步引入视觉-语言交错数据。视觉塔先单独训练，再解冻语言模型层，最终将视觉-文本数据比例提升至30%。
*   **视觉-语言冷却阶段**：使用高质量语言和视觉-语言数据集继续训练。合成数据在此阶段显著提升数学推理、知识任务和代码生成性能。采用混合方法：选择预训练子集并增加合成内容。
*   **长上下文激活阶段**：使用上采样的长上下文冷却数据训练，启用扩展序列处理。40%全注意力数据和60%部分注意力数据，RoPE频率设为1,000,000。序列长度从4,096逐步增加到131,072。

  

> \*文章翻译到此结束，感谢同学们的认真阅读，如发现有错误或疑问请在评论区留言。  
>   
> **\*翻译辛苦，码字不易，如果感觉有收获，欢迎赞同/喜欢/收藏本文，并关注楼主~**

原文地址
----

[](https://link.zhihu.com/?target=https%3A//github.com/MoonshotAI/Kimi-k1.5/blob/main/Kimi_k1.5.pdf)

本文转自 <https://zhuanlan.zhihu.com/p/19832470405>，如有侵权，请联系删除。