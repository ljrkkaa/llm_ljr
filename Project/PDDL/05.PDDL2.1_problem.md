参考资料：

https://planning.wiki/ref/pddl21/problem

PDDL 2.1 中的问题语法需要扩展，以补充领域文件中的语法。PDDL 2.1 语法的一个有趣扩展是添加一个度量标准。度量标准类似于优化函数，为计划定义一个成本值。然后我们表达是否希望这个度量标准被最大化（例如在足球中）或最小化（例如在高尔夫中）。

```
(define
    (problem rover1)
    (:domain rover-domain)
    (:objects
        r1 r2 - rover
        wp1 wp2 - waypoint
    )
    (:init
        (= (battery-amount r1) 100)
        (= (recharge-rate r1) 2.5)
        ...
    )
    (:goal (and
            ...
        )
    )
    (:metric maximize (+
            (battery-amount r1)
            (battery-amount r2)
        )
    )
)
```

# 1 Numeric Fluents   数值状态变量

```
    (= (<fluent_name> <argument>) <value>)
```

An equals prefix notation is used to assign a value to a numeric fluent. Some planners will require all possible numeric values for numeric fluents should be declared before it will attempt to plan.
使用等号前缀表示法为数值状态变量赋值。某些规划器会要求在尝试规划之前声明数值状态变量的所有可能数值。

```
    (= (battery-amount r1) 10)
```


# 2 Metric

```
    (:metric minimize (<numeric_operation>))
```

```
    (:metric maximize (<numeric_operation>))
```

指标可以使用对状态变量的数值运算来定义，包括仅使用单个状态变量。声明指标最常见的方法是声明一个无参数的数值状态变量，并在每个动作的效果中通过该动作的成本来增加它。

We can choose to either minimize or maximize the value of the metric function. Note, a metric function is NOT a replacement for a goal.
我们可以选择最小化或最大化指标函数的值。注意，指标函数不是目标的替代品。
